description: >
  This job to run simple python setups

parameters:
  install-deps:
    default: false
    type: boolean

executor: default

steps:
  - install-pipenv:
      and-deps: << parameters.install-deps >>
  - run: << parameters.test-steps >>
